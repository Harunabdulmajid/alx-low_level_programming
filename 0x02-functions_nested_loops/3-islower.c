#include "main.h"
/**
 * _islower - checks if character is lowercase
 *@c: argumemt passed
 * Return:returns I and O depending on condition
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
