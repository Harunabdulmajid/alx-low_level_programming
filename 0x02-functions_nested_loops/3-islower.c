#include "main.h"
/**
 * _islower - checks if character is lowercase
 *@c: a character argument
 * Return: returns I and O depending on conditio
 */

int _islower(int c);
{
	return (c >= 'a' && c <= 'z');
}
