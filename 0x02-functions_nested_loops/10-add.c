#include "main"
#include <stdio.h>
/**
 * add - add the value of two interger
 * @int: paramter
 * Return: Always 0
 */
int add(int, int)
{
	int (int x, int y);
	int x = 2;
	int y = 4;

	n = (x + y);
	printf("%d\n", n);
	return (0);
}
