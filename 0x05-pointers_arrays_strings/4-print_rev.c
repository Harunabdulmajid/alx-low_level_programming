#include "main.h"
#include <string.h>
/**
 * print_rev - funtion that prints a string
 * @s: string to be printed
 */

void print_rev(char *s)
{
	int i, n;
	
	n = strlen(s);
	for (i = n - 1; i >= 0; i--)
	{
	_putchar('\n');
	}
}

