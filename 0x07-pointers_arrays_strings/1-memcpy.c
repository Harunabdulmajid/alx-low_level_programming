#include "main.h"
#include <string.h>

/**
 * _memcpy - copy memory area
 * @dest: memory area
 * @src: param1
 * @n: interger array
 * Return: dest
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int b;

	for (b = 0; i < n; b++)

		dest[b] = src[b];

		return (dest);
}
