#include "main.h"
/**
 * main - test functions that prints if intergeris positive or negative.
 * Return: always 0
 */
int main(void)
{
	int i;

	i = 0;

	positive_or_negative(i);
	positive_negative;
	return (0);
}
