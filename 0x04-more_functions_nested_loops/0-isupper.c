#include "main.h"
/**
 * _isupper - prints upper alphabet
 * @c: interger parameter
 * Return: Something but has to be succes
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else 
		return (0);
}
