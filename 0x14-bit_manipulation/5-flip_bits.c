#include "main.h"

/**
 * flip_bits - returns the number of bit needed to filp
 * @n: number to flip
 * @m: number to flip again
 * Return: numbers flipped
 */
unsigned int flip_bits(unsigned long int n, unsigned long int m)
{
